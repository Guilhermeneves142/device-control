import { Meta } from "@storybook/addon-docs/blocks";

export const RadiusCard = ({ varName, px }) => (
  <div
    style={{
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      gap: "10px",
    }}
  >
    <div
      style={{
        width: "88px",
        height: "88px",
        backgroundColor: "var(--color-primary)",
        borderRadius: `var(${varName})`,
        boxShadow: "0 2px 8px rgba(0,0,0,0.08)",
      }}
      title={`${varName} — ${px}`}
    />
    <code style={{ fontSize: "11px", textAlign: "center", color: "var(--color-text-secondary)" }}>{varName}</code>
    <span style={{ fontSize: "11px", color: "var(--color-text-placeholder)", fontFamily: "monospace" }}>{px}</span>
  </div>
);

<Meta title="Design System/Tokens/Radius" />

# Radius (border-radius)

Tokens em `src/styles/tokens/radius.css`.

<div className="ds-radius" style={{ padding: "8px 0 32px" }}>

<div
  style={{
    display: "flex",
    flexWrap: "wrap",
    gap: "var(--spacing-8)",
    alignItems: "flex-end",
    marginTop: "16px",
    padding: "var(--spacing-6)",
    backgroundColor: "var(--color-surface)",
    border: "1px solid var(--color-border)",
    borderRadius: "var(--radius-lg)",
  }}
>
  <RadiusCard varName="--radius-sm" px="4px" />
  <RadiusCard varName="--radius-md" px="8px" />
  <RadiusCard varName="--radius-lg" px="12px" />
  <RadiusCard varName="--radius-xl" px="16px" />
</div>

## Uso típico

<ul style={{ marginTop: "20px", fontSize: "var(--font-size-sm)", color: "var(--color-text-secondary)", lineHeight: "var(--line-height-relaxed)", maxWidth: "520px" }}>
  <li><code>--radius-sm</code> — inputs, botões, chips</li>
  <li><code>--radius-md</code> — cartões pequenos, dropdowns</li>
  <li><code>--radius-lg</code> — painéis, modais</li>
  <li><code>--radius-xl</code> — ícones em destaque (ex.: InfoCard)</li>
</ul>

</div>
